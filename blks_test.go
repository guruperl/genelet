package genelet

import (
	"testing"
	// "net/url"
)

func TestSendmail(t *testing.T) {
	/*
			c := NewConfig(filename)
			lists := make([]map[string]interface{},0)
			ARGS  := make(url.Values)
			other := make(map[string]interface{})
			other["_gmail"] = map[string]interface{}{
		            "To":"peter@kinet.com",
		            "Subject":"Welcome to Wavelet Publisher",
		            "content":"SMTP testing email only"}
			err := c.Sendmail(lists, ARGS, other)
			if err != nil {
				t.Errorf("%v", err)
			}
	*/
}
